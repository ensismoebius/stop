# URLs do backend. Vazio = mesma origem da pagina.
# Em desenvolvimento o Vite ja faz proxy de /api e /socket.io para :3000,
# entao normalmente nao e preciso definir nada aqui.
VITE_API_URL=
VITE_SOCKET_URL=
